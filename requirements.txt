# Pinned to the `mvr` conda environment (Python 3.9).
#
# PyTorch is built for CUDA 12.1. Install the matching wheels first, e.g.:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
# then `pip install -r requirements.txt` for the rest.
torch==2.5.1
torchvision==0.20.1
timm==1.0.11
numpy==1.24.4
pandas==2.2.3
omegaconf==2.3.0
scikit-learn==1.5.2
h5py==3.12.1
pillow==10.4.0
tqdm==4.67.1
wandb==0.19.10
tensorboard==2.18.0

# Fast JPEG decoding for raw-bytes (.pkl) inputs. Requires the libturbojpeg system
# library. Installed from source (repo: https://github.com/lilohuang/PyTurboJPEG):
PyTurboJPEG @ git+https://github.com/lilohuang/PyTurboJPEG.git@v1.7.7
